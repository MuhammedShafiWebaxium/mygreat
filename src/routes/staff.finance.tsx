import {createFileRoute,redirect}from '@tanstack/react-router';import FinanceManagement from '@/screens/staff/FinanceManagement';import{getCurrentUserFn}from '@/features/auth/auth.functions';export const Route=createFileRoute('/staff/finance')({beforeLoad:async()=>{const u=await getCurrentUserFn();if(u?.role!=='SUPER_ADMIN')throw redirect({to:'/staff'})},component:FinanceManagement})
